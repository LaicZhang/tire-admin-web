# 平台本地运行端口号
VITE_PORT = 8848

# 是否隐藏首页 隐藏 true 不隐藏 false （勿删除，VITE_HIDE_HOME只需在.env文件配置）
VITE_HIDE_HOME = false

VITE_SERVER_URL = "http://localhost:3000"

# 开发环境 Vite 代理目标（仅 dev server 生效；默认回落到 VITE_SERVER_URL）
VITE_PROXY_TARGET = "http://localhost:3000"
