# pnpm 配置存放于 .npmrc（pnpm/npm 均会读取，避免在本项目中混用 npm/yarn）
# 本地建议开启，以确保 `preinstall`(only-allow pnpm) 与 `prepare`(husky) 正常生效
# CI 如需禁用，可使用 `pnpm config set enable-pre-post-scripts false` 或设置 `PNPM_ENABLE_PRE_POST_SCRIPTS=false`
shell-emulator=true
shamefully-hoist=true
enable-pre-post-scripts=true
strict-peer-dependencies=false
